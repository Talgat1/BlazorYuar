@using BlazorYuar.Data
@page "/hello"

<h1>Выберите список</h1>
<button class="btn btn-primary" @onclick="stud">Cписок студентов</button>
@if (studlist == null)
{
    <p><em>Loading...</em></p>
}
else if (gamelist == null && carlist == null)
{
    <table class="table">
        <thead>
            <tr>
                <th>Фамилия</th>
                <th>Имя</th>
                <th>Возраст</th>
                <th>Курс</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var studet in studlist)
            {
                <tr>
                    <td>@studet.Sur</td>
                    <td>@studet.Name</td>
                    <td>@studet.Year</td>
                    <td>@studet.Faculty</td>
                </tr>
            }
        </tbody>
    </table>  
}

<button class="btn btn-primary" @onclick="carad">Cписок машин</button>
@if (carlist == null)
{
    <p><em>Loading...</em></p>
}
else if (gamelist == null && studlist == null)
{
    <table class="table">
        <thead>
            <tr>
                <th>Название</th>
                <th>Марка</th>
                <th>Дата выпуска</th>
                <th>Страна</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var carad in carlist)
            {
                <tr>
                    <td>@carad.Name</td>
                    <td>@carad.Brand</td>
                    <td>@carad.YeaarRelease</td>
                    <td>@carad.Country</td>
                </tr>
            }
        </tbody>
    </table>
}

    <button class="btn btn-primary" @onclick="gem">Cписок игр</button>
    @if (gamelist == null)
    {
        <p><em>Loading...</em></p>
    }
    else if (studlist == null && carlist == null)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Название</th>
                    <th>Год выпуска</th>
                    <th>Категория</th>
                    <th>Страна</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var gem in gamelist)
                {
                    <tr>
                        <td>@gem.Name</td>
                        <td>@gem.Year</td>
                        <td>@gem.Category</td>
                        <td>@gem.Country</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    @code {
        public static List<Student> studlist;
        public static List<Car> carlist;
        public static List<Game> gamelist;
        //int n = 0;
        private void stud()
        {                     
            studlist = Student.AddSt();
            //n++;
            carlist = null;
            gamelist = null;
            
        }

        private void carad()
        {           
            carlist = Car.AddCar();
            gamelist = null;
            studlist = null;

        }
        private void gem()
        {            
            gamelist = Game.AddGem();
            carlist = null;            
            studlist = null;
        }
}
